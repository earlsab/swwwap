// TEMP DATA

const users = [{ id: 1, name: "EJ" }];

const items = [{ id: 1, title: "iPhone Pro Max" }];

export const getPosts = async () => {
  return postcss;
};

export { users, items };
// export const getPost = aysnc (id) => {

// }
